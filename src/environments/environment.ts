export const environment = {
  production: false,
  // URL LOCAL de funciones (cuando sirves con `npx supabase functions serve ...`)
  apiUrl: "http://127.0.0.1:54321/functions/v1/submit-screening",
}
